import React from 'react';
import { ChefHat, Pizza, Coffee, Utensils, Truck } from 'lucide-react';

const Loader = () => {
    return (
        <h1 className="my-4 text-center">Loading....</h1>
    );
};

export default Loader;